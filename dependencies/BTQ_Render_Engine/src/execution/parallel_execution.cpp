#include "../include/execution/parallel_execution.hpp"

namespace btq {
namespace execution {

// The implementations are header-only for now, but we can add more complex implementations here if needed

} // namespace execution
} // namespace btq