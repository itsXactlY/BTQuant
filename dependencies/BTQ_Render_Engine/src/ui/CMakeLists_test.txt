cmake_minimum_required(VERSION 3.15)
project(TestEmptyStates)

set(CMAKE_CXX_STANDARD 26)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Find Dependencies
find_package(Threads REQUIRED)

# External Headers
include(FetchContent)
FetchContent_Declare(imgui GIT_REPOSITORY https://github.com/ocornut/imgui.git GIT_TAG docking)
FetchContent_Declare(json GIT_REPOSITORY https://github.com/nlohmann/json.git GIT_TAG v3.11.2)

FetchContent_MakeAvailable(imgui json)

# Path Configuration
set(IMGUI_DIR ${imgui_SOURCE_DIR})

# Define sources
set(TEST_SOURCES
    test_empty_states_standalone.cpp
    empty_states.cpp
    ${IMGUI_DIR}/imgui.cpp
    ${IMGUI_DIR}/imgui_draw.cpp
    ${IMGUI_DIR}/imgui_tables.cpp
    ${IMGUI_DIR}/imgui_widgets.cpp
)

add_executable(test_empty_states ${TEST_SOURCES})

target_include_directories(test_empty_states PRIVATE
    ../../include
    ${IMGUI_DIR}
    ${json_SOURCE_DIR}/include
)

target_link_libraries(test_empty_states PRIVATE
    Threads::Threads
)